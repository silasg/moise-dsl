# This patch file was generated by NetBeans IDE
# It uses platform neutral UTF-8 encoding and \n newlines.
--- Base (BASE)
+++ Locally Modified (Based On LOCAL)
@@ -206,9 +206,15 @@
         }
         this.target =  destination;
 
-        setScope(ele.getAttribute("scope"));
-        setExtendsToSubGroups( Boolean.valueOf(ele.getAttribute("extends-sub-groups")).booleanValue());
-        setBiDir( Boolean.valueOf(ele.getAttribute("bi-dir")).booleanValue() );
+        String relScope = ele.getAttribute("scope");
+        if (!relScope.isEmpty())
+            setScope(relScope);
+        String subGroups = ele.getAttribute("extends-sub-groups");
+        if (!subGroups.isEmpty())
+            setExtendsToSubGroups(Boolean.valueOf(subGroups).booleanValue());
+        String biDir = ele.getAttribute("bi-dir");
+        if (!biDir.isEmpty())
+            setBiDir( Boolean.valueOf(biDir).booleanValue());
         setId();
     }    
 }
